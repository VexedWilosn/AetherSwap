# Changelog

## v1.0.0-docker

### 变更
- 付款确认后，在创建待收货记录并提醒卖家发货后，会立即启动一轮短周期后台收货尝试。
- 收货 worker 不再等待主流程离开稳定性检测或选品阶段后才处理待收货记录，避免主流程长时间运行导致收货卡住。
- 收货尝试现在使用专用收货锁串行化，避免付款后即时收货和周期 worker 同时接受同一个 Steam 报价。
- 单笔订单在用户确认付款后，会先轮询 Buff 待付款列表；订单仍处于待付款时不会写入待收货记录。
- 每次待付款提示都会带有独立 `payment_id`，手动确认和邮件确认都会绑定该 ID，避免旧邮件线程或旧按钮确认串到新订单。
- 收货成功后会主动刷新库存缓存，减少“持有饰品已更新但库存管理仍显示旧数据”的情况。
- 仪表盘新增“立即收货”按钮，可手动触发待收货报价处理。
- 库存接口会返回当前结果是否来自缓存，库存页会显示缓存/实时刷新提示。

### 修复
- 收货失败时会记录更明确的日志，包括 Steam Cookie 缺失、Buff Cookie 缺失、Buff 待收货接口失败、Steam 空响应、Steam 非 JSON 响应、Steam `strError`、库存扫描失败、本地待收货记录未匹配等情况。
- Steam 报价接受成功并匹配到本地购买记录后，会记录清晰的待收货记录更新日志。
- Steam 自动登录和库存刷新相关错误在前端会转成更容易理解的提示，例如 Cookie 缺失、网络/代理异常、请求重试耗尽。

### 说明
- 该改动用于减少已付款 BUFF 订单在主流程持续稳定性检测时长时间停留在 `待收货` 的情况。
- 批量微信路径目前仍需先完成批量冻结/核销后才能拿到单个 `bill_order_id`；本次订单状态校验优先覆盖单笔订单支付流程。
